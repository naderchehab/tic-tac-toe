
$(function () {

    // var model = new TicTacToe.Board();

    // var move = 16;
    // model.set("xMarks", 321);
    // model.set("oMarks", 10);
    // model.set("currentPlayer", "o");

    // var score = TicTacToe.AlphaBetaStrategy.getScore(move, model, false, 0, -1000, 1000);
    // console.log("16:", score);


    // move = 128;
    // model.set("xMarks", 321);
    // model.set("oMarks", 10);
    // model.set("currentPlayer", "o");

    // score = TicTacToe.AlphaBetaStrategy.getScore(move, model, false, 0, -1000, 1000);
    // console.log("128:", score);
});
